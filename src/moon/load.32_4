(DEFUN LOAD-32.4 ()
  (DELETE-INITIALIZATION "EVAL" NIL 'CHAOS:SERVER-ALIST)
  (DELETE-INITIALIZATION "CPT-SYNC" NIL 'SYS:SYSTEM-INITIALIZATION-LIST)
  (LOAD "LMFONT;MEDFNB")
  (SETQ TV:MAIN-SCREEN TV:CPT-SCREEN)
  (SETF (TV:SHEET-NAME TV:MAIN-SCREEN) "MAIN-SCREEN")
  (LET ((SI:PACKAGES-INITIALLY-LOADED '(SYSTEM-INTERNALS FORMAT
					CHAOS COMPILER QFASL-REL TIME
					TV SUPDUP FED COLOR EH CADR PRESS MICRO-ASSEMBLER)))
    (QLD NIL ':CONFIRM)))
